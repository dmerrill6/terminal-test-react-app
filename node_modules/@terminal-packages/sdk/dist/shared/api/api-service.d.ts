import { EnvironmentTypes } from '../enums/environment-types';
import { ISaveLogRequest } from './requests/isave-log.request';
export declare class ApiService {
    private _apiKey;
    private _environment;
    constructor(_apiKey: string, _environment: EnvironmentTypes);
    /**
     * Save to logs
     */
    saveToLogs(saveLogRequest: ISaveLogRequest): Promise<void>;
    /**
     * get the environment url
     */
    readonly environmentUrl: string;
}
